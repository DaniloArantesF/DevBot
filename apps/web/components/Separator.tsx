import * as SeparatorPrimitive from '@radix-ui/react-separator';
import classes from '@styles/Separator.module.css';

const Separator = () => <SeparatorPrimitive.Root className={classes.root} />;

export default Separator;
